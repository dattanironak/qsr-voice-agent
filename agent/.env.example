# LiveKit Cloud (same project as the backend/frontend — used to join the room and to
# authenticate with the hosted STT/LLM/TTS inference gateway).
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Base URL of the backend (menu lookups, order creation, conversation recording).
BACKEND_URL=http://localhost:8000
